#include <bits/stdc++.h>
using namespace std;
int arr[100100];
int mp[2020];
int main()	{
	int t;
	cin>>t;
	while(t--)	{
		cin>>n;
		memset(mp,0,sizeof(mp));
		for(int i=0;i<n;i++)	{
			cin>>arr[i];
			mp[arr[i]+1000]++;
		}
		long long ans=0;
		for(int i=0;i<=2000;i++)	{
			for(int j=)
		}
	}
	return 0;
}